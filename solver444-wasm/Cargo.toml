[package]
name = "solver444-wasm"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Dependency-free 4x4x4 cube state and move engine for cubetimer"

[lib]
crate-type = ["rlib", "cdylib"]

[profile.release]
lto = true
codegen-units = 1
opt-level = "s"
