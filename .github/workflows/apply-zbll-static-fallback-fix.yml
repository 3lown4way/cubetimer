name: Apply ZBLL static fallback fix

on:
  push:
    branches:
      - fix/zbll-static-index-fallback

permissions:
  contents: write

jobs:
  patch:
    if: ${{ !contains(github.event.head_commit.message, '[zbll-auto-patch]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/zbll-static-index-fallback
          fetch-depth: 0

      - name: Apply source patch
        run: node tools/apply-zbll-static-fallback-fix.mjs

      - name: Remove one-time patch machinery
        run: |
          git rm .github/workflows/apply-zbll-static-fallback-fix.yml
          git rm tools/apply-zbll-static-fallback-fix.mjs

      - name: Commit patched source
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add solver/cfop3x3.js
          git commit -m "[zbll-auto-patch] Fall back when static ZBLL lookup misses"
          git push origin HEAD:fix/zbll-static-index-fallback
