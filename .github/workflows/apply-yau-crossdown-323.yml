name: Apply Yau cross-down 3-2-3

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/apply-yau-crossdown-323.yml"
      - "tools/apply-yau-crossdown-323.py"

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Apply cross-down patch
        run: python tools/apply-yau-crossdown-323.py

      - name: Syntax check
        run: |
          node --check solver/solver444.js
          node --check solver/edgePairing444.js

      - name: Verify Yau and 3-2-3 regressions
        run: |
          node tools/verify-444-yau.mjs
          node tools/verify-444-edge-323.mjs
          node tools/verify-444-public-notation.mjs
          git diff --check

      - name: Commit implementation
        run: |
          rm -f tools/apply-yau-crossdown-323.py
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add solver/solver444.js tools/apply-yau-crossdown-323.py
          git commit -m "Keep Yau cross down through 3-2-3"
          git push origin HEAD:main
